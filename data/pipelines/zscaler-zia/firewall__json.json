{
  "id": "dm_zscaler_zia_firewall_json",
  "conf": {
    "output": "default",
    "description": "Zscaler Internet Access (ZIA) firewall / json: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "zscaler-zia / firewall / json. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Zscaler ZIA integration (zscaler_zia), firewall data stream). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'zscaler_zia.firewall'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
