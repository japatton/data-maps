{
  "id": "dm_tanium_threat_response_syslog_cef",
  "conf": {
    "output": "default",
    "description": "Tanium threat-response / syslog-cef: thin pass-through, parsing deferred to Elastic (elastic-ingest-pipeline).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "tanium / threat-response / syslog-cef. Parsing is deliberately NOT implemented here: the Elastic ingest pipeline decodes this downstream (generic CEF decode + logs-tanium.threat_response@custom (no CEF path in the stock Tanium integration)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'tanium.threat_response'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
