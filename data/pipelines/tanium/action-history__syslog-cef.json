{
  "id": "dm_tanium_action_history_syslog_cef",
  "conf": {
    "output": "default",
    "description": "Tanium action-history / syslog-cef: thin pass-through, parsing deferred to Elastic (elastic-ingest-pipeline).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "tanium / action-history / syslog-cef. Parsing is deliberately NOT implemented here: the Elastic ingest pipeline decodes this downstream (generic CEF decode + logs-tanium.action_history@custom (no CEF path in the stock Tanium integration)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'tanium.action_history'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
