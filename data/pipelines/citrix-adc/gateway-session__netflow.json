{
  "id": "dm_citrix_adc_gateway_session_netflow",
  "conf": {
    "output": "default",
    "description": "Citrix ADC (NetScaler) gateway-session / netflow: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "citrix-adc / gateway-session / netflow. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Elastic NetFlow Records integration (netflow), data stream logs-netflow.log). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'citrix_adc.gateway_session'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
