{
  "id": "dm_servicenow_change_request_webhook",
  "conf": {
    "output": "default",
    "description": "ServiceNow change request (change_request), webhook via Business Rule outbound REST on state/approval change: JSON extract + ECS/custom field mapping, direct route.",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "ServiceNow change request (change_request) via Business Rule outbound webhook on state/approval change. Direct route. state may arrive as a code or a label depending on how the Business Rule was authored; this ambiguity cannot be resolved generically, so the raw value is passed through to servicenow.change.state (see README gap)."
        },
        "description": "pipeline purpose"
      },
      {
        "id": "serde",
        "filter": "true",
        "conf": {
          "mode": "extract",
          "type": "json",
          "srcField": "_raw"
        },
        "description": "Cribl HTTP Source receives the POST body; extract fields from _raw"
      },
      {
        "id": "rename",
        "filter": "true",
        "conf": {
          "rename": [
            {
              "currentName": "sys_id",
              "newName": "'event.id'"
            },
            {
              "currentName": "short_description",
              "newName": "message"
            }
          ]
        },
        "description": "straightforward 1:1 field copies"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "'servicenow.change.number'",
              "value": "__e['number']"
            },
            {
              "name": "'servicenow.change.state'",
              "value": "__e['state']"
            },
            {
              "name": "'servicenow.change.approval'",
              "value": "__e['approval']"
            },
            {
              "name": "'user.name'",
              "value": "__e['assigned_to']"
            },
            {
              "name": "'servicenow.change.cmdb_ci'",
              "value": "__e['cmdb_ci']"
            },
            {
              "name": "'event.outcome'",
              "value": "__e['close_code']==='successful' ? 'success' : (__e['close_code']==='unsuccessful' ? 'failure' : 'unknown')"
            },
            {
              "name": "'servicenow.change.close_code_raw'",
              "value": "__e['close_code']"
            },
            {
              "name": "_time",
              "value": "Date.parse(__e['sys_updated_on'].replace(' ','T')+'Z')/1000"
            },
            {
              "name": "'event.dataset'",
              "value": "'servicenow.change_request'"
            }
          ],
          "remove": [
            "number",
            "state",
            "approval",
            "assigned_to",
            "cmdb_ci",
            "close_code",
            "sys_updated_on"
          ]
        },
        "description": "map the correlation columns to servicenow.change.*, derive event.outcome from close_code while retaining the raw value, set _time from sys_updated_on, tag event.dataset"
      },
      {
        "id": "code",
        "filter": "true",
        "description": "Re-nest flat dotted keys (data-maps canonical step)",
        "conf": {
          "maxNumOfIterations": 5000,
          "code": "const ks = Object.keys(__e).filter(k => !k.startsWith('__') && k.indexOf('.') > 0 && k.split('.').every(s => s !== '')).sort((a, b) => a.split('.').length - b.split('.').length);\nfor (const k of ks) {\n  const parts = k.split('.');\n  const leaf = parts.pop();\n  let t = __e;\n  let ok = true;\n  for (const p of parts) {\n    const v = t[p];\n    if (v !== undefined && (v === null || typeof v !== 'object' || Array.isArray(v))) { ok = false; break; }\n    t = v === undefined ? null : v;\n    if (t === null) break;\n  }\n  if (!ok) continue;\n  t = __e;\n  for (const p of parts) { if (t[p] === undefined) t[p] = {}; t = t[p]; }\n  t[leaf] = __e[k];\n  delete __e[k];\n}"
        }
      }
    ]
  }
}
