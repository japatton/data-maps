{
  "id": "dm_hashicorp_vault_audit_json",
  "conf": {
    "output": "default",
    "description": "HashiCorp Vault audit / json: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "hashicorp-vault / audit / json. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Hashicorp Vault integration (audit data stream)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'hashicorp_vault.audit'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
