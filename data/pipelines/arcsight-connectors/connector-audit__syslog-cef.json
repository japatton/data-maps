{
  "id": "dm_arcsight_connectors_connector_audit_syslog_cef",
  "conf": {
    "output": "default",
    "description": "ArcSight SmartConnectors / Connector Appliance (transitional) connector-audit / syslog-cef: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "arcsight-connectors / connector-audit / syslog-cef. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Common Event Format (CEF) integration). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'arcsight_connectors.connector_audit'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
