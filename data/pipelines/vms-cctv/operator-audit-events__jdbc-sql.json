{
  "id": "dm_vms_cctv_operator_audit_events_jdbc_sql",
  "conf": {
    "output": "default",
    "description": "VMS operator audit, Genetec Data Exporter plugin PostgreSQL audit-events view (config-change subset only) via Cribl database collector. The work order deliberately leaves fields[] empty because the plugin guide does not publish the view's PostgreSQL column names, so no rename/transform is implemented here -- only event.dataset is set. See README.",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "vms-cctv operator-audit-events, jdbc-sql (Genetec Data Exporter plugin -> PostgreSQL audit-events view, Cribl DB collector). PostgreSQL column names are NOT published; no field mapping implemented -- see README gap. Config-change subset only, run alongside the SDK api-pull branch."
        },
        "description": "Identify pipeline scope and document the unresolved schema"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "'event.dataset'",
              "value": "'vms_cctv.operator_audit_events'"
            }
          ]
        },
        "description": "Only transform implementable without guessing an unpublished column schema"
      },
      {
        "id": "code",
        "filter": "true",
        "description": "Re-nest flat dotted keys (data-maps canonical step)",
        "conf": {
          "maxNumOfIterations": 5000,
          "code": "const ks = Object.keys(__e).filter(k => !k.startsWith('__') && k.indexOf('.') > 0 && k.split('.').every(s => s !== '')).sort((a, b) => a.split('.').length - b.split('.').length);\nfor (const k of ks) {\n  const parts = k.split('.');\n  const leaf = parts.pop();\n  let t = __e;\n  let ok = true;\n  for (const p of parts) {\n    const v = t[p];\n    if (v !== undefined && (v === null || typeof v !== 'object' || Array.isArray(v))) { ok = false; break; }\n    t = v === undefined ? null : v;\n    if (t === null) break;\n  }\n  if (!ok) continue;\n  t = __e;\n  for (const p of parts) { if (t[p] === undefined) t[p] = {}; t = t[p]; }\n  t[leaf] = __e[k];\n  delete __e[k];\n}"
        }
      }
    ]
  }
}
