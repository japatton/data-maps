{
  "id": "dm_vms_cctv_operator_audit_events_otlp",
  "conf": {
    "output": "default",
    "description": "VMS operator audit, XProtect 2026 R1+ native OTLP audit-log export (API Gateway, OpenTelemetry LogRecord model). Maps the transport-guaranteed OTLP envelope; the XProtect-specific User/camera/action/filename content is NOT published as structured Attributes in the white paper, so this branch carries only the envelope fields -- see README for the Body-regex gap shared with the api-pull branch.",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "vms-cctv operator-audit-events, otlp (XProtect 2026 R1+ API Gateway -> Cribl OTLP source). OTLP LogRecord envelope only; Attributes schema unpublished, see README."
        },
        "description": "Identify pipeline scope"
      },
      {
        "id": "rename",
        "filter": "true",
        "conf": {
          "rename": [
            {
              "currentName": "Body",
              "newName": "message"
            }
          ]
        },
        "description": "Batch rename of the one clean, status:mapped source name (OTLP Body)"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "__dm_time",
              "value": "__e['Timestamp'] ? __e['Timestamp']/1000000000 : __e['_time']"
            },
            {
              "name": "_time",
              "value": "__e['__dm_time'] !== undefined && __e['__dm_time'] !== null && __e['__dm_time'] === __e['__dm_time'] ? __e['__dm_time'] : __e['_time']"
            },
            {
              "name": "'event.created'",
              "value": "__e['ObservedTimestamp'] ? new Date(__e['ObservedTimestamp']/1000000).toISOString() : __e['event.created']"
            },
            {
              "name": "'log.level'",
              "value": "__e['SeverityText'] ? (/^WARN/i.test(__e['SeverityText']) ? 'warning' : (/^FATAL/i.test(__e['SeverityText']) ? 'critical' : __e['SeverityText'].toLowerCase())) : __e['log.level']"
            },
            {
              "name": "'event.severity'",
              "value": "__e['SeverityNumber'] ? __e['SeverityNumber'] : __e['event.severity']"
            },
            {
              "name": "'vms.otlp.resource'",
              "value": "__e['Resource']"
            },
            {
              "name": "'vms.otlp.attributes'",
              "value": "__e['Attributes']"
            },
            {
              "name": "'event.dataset'",
              "value": "'vms_cctv.operator_audit_events'"
            }
          ]
        },
        "description": "OTLP envelope: nanosecond timestamps to _time/event.created, SeverityText/Number normalization, raw Resource/Attributes preserved pending live-sample schema, event.dataset"
      },
      {
        "id": "code",
        "filter": "true",
        "description": "Re-nest flat dotted keys (data-maps canonical step)",
        "conf": {
          "maxNumOfIterations": 5000,
          "code": "const ks = Object.keys(__e).filter(k => !k.startsWith('__') && k.indexOf('.') > 0 && k.split('.').every(s => s !== '')).sort((a, b) => a.split('.').length - b.split('.').length);\nfor (const k of ks) {\n  const parts = k.split('.');\n  const leaf = parts.pop();\n  let t = __e;\n  let ok = true;\n  for (const p of parts) {\n    const v = t[p];\n    if (v !== undefined && (v === null || typeof v !== 'object' || Array.isArray(v))) { ok = false; break; }\n    t = v === undefined ? null : v;\n    if (t === null) break;\n  }\n  if (!ok) continue;\n  t = __e;\n  for (const p of parts) { if (t[p] === undefined) t[p] = {}; t = t[p]; }\n  t[leaf] = __e[k];\n  delete __e[k];\n}"
        }
      }
    ]
  }
}
