{
  "id": "dm_cisco_ftd_intrusion_syslog_kv",
  "conf": {
    "output": "default",
    "description": "Cisco Secure Firewall Threat Defense (FTD) intrusion / syslog-kv: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "cisco-ftd / intrusion / syslog-kv. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Cisco FTD integration (cisco_ftd)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'cisco_ftd.intrusion'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
