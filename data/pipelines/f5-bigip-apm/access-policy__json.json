{
  "id": "dm_f5_bigip_apm_access_policy_json",
  "conf": {
    "output": "default",
    "description": "F5 BIG-IP APM access-policy / json: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "f5-bigip-apm / access-policy / json. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Elastic f5_bigip integration, data stream logs-f5_bigip.log (Agent HTTP endpoint, TS JSON)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'f5_bigip_apm.access_policy'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
