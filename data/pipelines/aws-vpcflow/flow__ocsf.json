{
  "id": "dm_aws_vpcflow_flow_ocsf",
  "conf": {
    "output": "default",
    "description": "AWS GovCloud VPC Flow Logs flow / ocsf: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "aws-vpcflow / flow / ocsf. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Elastic Amazon Security Lake integration (OCSF), S3/SQS input). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'aws_vpcflow.flow'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
