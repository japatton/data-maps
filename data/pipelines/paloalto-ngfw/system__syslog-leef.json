{
  "id": "dm_paloalto_ngfw_system_syslog_leef",
  "conf": {
    "output": "default",
    "description": "Palo Alto NGFW System log (syslog-leef, PAN-OS 10.2/11.1), direct route: recommendations.direct.cribl for this exact format/dataset says 'High-side instances pass LEEF through with Persistent Queue backpressure and parse at the core group' - explicitly NOT a field-level Cribl parse, diverging from this map's other Palo Alto LEEF pipelines (per brief rule 4, followed here). Tags event.dataset only; the LEEF payload is left untouched in _raw for the downstream parser.",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "System log, syslog-leef, direct route. recommendations.direct.cribl (verbatim): 'High-side instances pass LEEF through with Persistent Queue backpressure and parse at the core group.' Unlike the sibling globalprotect/threat/traffic/userid LEEF pipelines in this directory (regex_extract header + serde kvp attributes), this dataset's own recommendation explicitly instructs NOT parsing here - per brief rule 4 this outranks the generic recipe. EventID/EventDescription/Severity/LogSourceName/DeviceSN/SequenceNo/SubType/VirtualLocation are left in _raw for the Elastic-side integration named in the recommendation."
        }
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'paloalto_ngfw.system'"
            }
          ]
        },
        "description": "Tag event.dataset for routing/observability only; no LEEF header or attribute parse per the recommendation's explicit pass-through instruction"
      }
    ]
  }
}
