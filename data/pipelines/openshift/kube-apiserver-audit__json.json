{
  "id": "dm_openshift_kube_apiserver_audit_json",
  "conf": {
    "output": "default",
    "description": "Red Hat OpenShift / Kubernetes audit kube-apiserver-audit / json: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "openshift / kube-apiserver-audit / json. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Kubernetes integration (audit_logs data stream)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'openshift.kube_apiserver_audit'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
