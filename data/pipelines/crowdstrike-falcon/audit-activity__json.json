{
  "id": "dm_crowdstrike_falcon_audit_activity_json",
  "conf": {
    "output": "default",
    "description": "CrowdStrike Falcon audit-activity / json: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "crowdstrike-falcon / audit-activity / json. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (CrowdStrike integration, falcon data stream). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'crowdstrike_falcon.audit_activity'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
