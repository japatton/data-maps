{
  "id": "dm_proofpoint_tap_clicks_api_pull",
  "conf": {
    "output": "default",
    "description": "Proofpoint Email Security tap-clicks / api-pull: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "proofpoint / tap-clicks / api-pull. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Proofpoint TAP integration (clicks_blocked / clicks_permitted data streams)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'proofpoint.tap_clicks'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
