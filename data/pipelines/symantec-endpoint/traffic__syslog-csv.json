{
  "id": "dm_symantec_endpoint_traffic_syslog_csv",
  "conf": {
    "output": "default",
    "description": "Symantec Endpoint Protection traffic / syslog-csv: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "symantec-endpoint / traffic / syslog-csv. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (elastic/integrations symantec_endpoint, data stream log). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'symantec_endpoint.traffic'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
