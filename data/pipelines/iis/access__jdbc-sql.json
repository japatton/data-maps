{
  "id": "dm_iis_access_jdbc_sql",
  "conf": {
    "output": "default",
    "description": "Microsoft IIS access / jdbc-sql: thin pass-through, parsing deferred to Elastic (elastic-ingest-pipeline).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "iis / access / jdbc-sql. Parsing is deliberately NOT implemented here: the Elastic ingest pipeline decodes this downstream (logs-iis.access@custom (column rename-to-ECS; collected by an SQL/JDBC input, not a file tail)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'iis.access'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
