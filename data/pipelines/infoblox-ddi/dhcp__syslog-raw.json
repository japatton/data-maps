{
  "id": "dm_infoblox_ddi_dhcp_syslog_raw",
  "conf": {
    "output": "default",
    "description": "Infoblox NIOS DDI dhcp / syslog-raw: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "infoblox-ddi / dhcp / syslog-raw. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (Elastic infoblox_nios integration (pipeline_dhcp)). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'infoblox_ddi.dhcp'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
