{
  "id": "dm_symantec_proxysg_access_main_csv_file",
  "conf": {
    "output": "default",
    "description": "Symantec ProxySG / Edge SWG access-main / csv-file: thin pass-through, parsing deferred to Elastic (elastic-integration).",
    "functions": [
      {
        "id": "comment",
        "filter": "true",
        "conf": {
          "comment": "symantec-proxysg / access-main / csv-file. Parsing is deliberately NOT implemented here: the Elastic integration is the parser downstream (elastic/integrations proxysg, data stream log, logfile input). Re-implementing that parsing in Cribl would fight it and yield duplicate or conflicting fields, so this pipeline only identifies the event and tags event.dataset, then passes _raw through unmodified. Direct route (no cross-domain guard hop)."
        },
        "description": "record why this pipeline does not parse"
      },
      {
        "id": "eval",
        "filter": "true",
        "conf": {
          "add": [
            {
              "name": "event.dataset",
              "value": "'symantec_proxysg.access_main'"
            }
          ]
        },
        "description": "tag the dataset before Elastic parses _raw"
      }
    ]
  }
}
